module.exports = {
  theme: {
    extend: {
      colors: {
        // Define custom colors here
        brand: {
          light: '#3b82f6', // Example light blue
          DEFAULT: '#1e40af', // Example blue
          dark: '#1e3a8a', // Example dark blue
        },
      },
      animation: {
        // Define custom animations here
        bounce50: 'bounce 0.5s infinite',
        spinSlow: 'spin 3s linear infinite',
      },
    },
  },
  variants: {},
  plugins: [],
};
